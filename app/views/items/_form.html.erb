<%= form_for [list, list.items.new ], remote: true do |f| %>
  <%= f.label :comment %>
  <%= f.text_are :name, class: 'form-control', placeholder: "Enter item." %>
  <div class="form-group">
  <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>